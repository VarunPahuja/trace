// Shared between ArrayRenderer and WindowOverlay so the overlay band lines
// up with actual cell positions — keep in sync with ArrayRenderer's cell
// classes (w-10 = 40px, gap-1 = 4px).
export const ARRAY_CELL_SIZE_PX = 40;
export const ARRAY_CELL_GAP_PX = 4;
